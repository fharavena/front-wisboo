import { resunsplashResult } from "./resunsplashResult";
export class unsplashResult {

    constructor(
        public total: Number,
        public total_pages: Number,
        public resultados: resunsplashResult
    ) { }
}